import { ImmediateClick.DirectiveDirective } from './immediate-click.directive';

describe('ImmediateClick.DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ImmediateClick.DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
